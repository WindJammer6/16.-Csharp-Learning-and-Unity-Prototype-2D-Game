using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class o_GetComponent_Unity_function_SelfmadeComponentOne_supporting_file : MonoBehaviour
{

    // This is a public variable 'playerScore' that we will attempt to access by using the 
    // 'GetComponent<>()' function on this C# script (custom component) in the 
    // 'n_GetComponent_Unity_function_and_creating_new_GameObjects.cs' file
    public int playerScore = 9001;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

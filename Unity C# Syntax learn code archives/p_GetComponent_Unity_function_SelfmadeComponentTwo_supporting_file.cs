using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class p_GetComponent_Unity_function_SelfmadeComponentTwo_supporting_file : MonoBehaviour
{

    // This is a public variable 'numberOfPlayerDeaths' that we will attempt to access by using the 
    // 'GetComponent<>()' function on this C# script (custom component) in the 
    // 'n_GetComponent_Unity_function_and_creating_new_GameObjects.cs' file
    public int numberOfPlayerDeaths = 3;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

namespace Project
{
    class Program
    {
        static void Main(string[] args)
        {
            //'Console.WriteLine' function writes a line of text in a console (note that
            //console is not the terminal that shows your usual outputs in the VScode,
            //but is a seperate window where your outputs will be shown in!)

            //It is almost equivalent the 'print' function in Python
            Console.WriteLine("Hello World");
        }
    }
}

